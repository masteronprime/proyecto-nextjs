/** @type {import('next').NextConfig} */
const nextConfig = {
  images:{
    domains:[
      "res.cloudinary.com",
    ]
  }
}

module.exports = nextConfig
const dotenv = require('dotenv');
dotenv.config();
